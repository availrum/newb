#include<iostream>
using namespace std;
int main(){//11719
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    string st;
    while(getline(cin,st)){
        cout<<st<<'\n';
        // if(st=="00") break;
    }
}